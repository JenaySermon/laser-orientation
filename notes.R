download packages - tidyverse 
have to call it when writing

functions - verbs mean()
objects - nouns of R language mean(student_grades)
arguments - adverbs or direct objects mean (student_grades, na.rm = FALSE)

control/enter is help menu

punctuation or operator. average_grade <- mean(student_grades, na.rm = FALSE) 
the pipe operator is the way to passing along information into the next function. 